# testv

Удалённый репозиторий: `https://github.com/oblikfeo/testv.git`  
Каталог `vendor/`, `node_modules/` и сборка Vite в репозиторий не входят — ставятся **только на сервере** после `git clone` / `git pull`.

Деплой: каталог **`deploy/`**.

- Новый VPS по IP `195.133.198.70`: `server-bootstrap.sh`, `server-deploy.sh`, `nginx-site-ip.conf` — см. `deploy/README.md`.
- Старый домен `avavpn.ru`: `nginx-avavpn.ru.conf`.
